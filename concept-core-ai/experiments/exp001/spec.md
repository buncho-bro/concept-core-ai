# Experiment 001 実験仕様書

## 潜在空間における共通構造の形成

# 0. 概念核について

## 0.1 背景

人間は、異なる対象や状況の間に共通する概念を認識できる。

例えば「赤」という概念を考える。

人間は、

- 赤いリンゴ
- 赤い苺
- 赤い車
- 赤い光
- 赤い図形

など、互いに異なる対象について「赤」という共通性を認識できる。

さらに、

```text id="m8r23s"
リンゴの赤
     │
苺の赤
     │
車の赤
     │
光の赤
     │
未知の物体の赤
```

のように、対象そのものが未知であっても「赤」という性質を再利用して理解することができる。

本研究では、このような多数の異なる経験に共通し、別の状況でも再利用できる内部的な構造を仮に**「概念核」**と呼ぶ。

---

## 0.2 概念核と名称は同一ではない

概念核そのものと、人間がそれに付けた名称は区別する。

例えば、

```text id="u45hbr"
              日本語「赤」
                   ↑
英語 "red" ←【概念核 R】→ 視覚的な赤
                   ↓
              その他の表現
```

という関係を想定する。

したがって、

```text id="w2d54u"
概念核 R ≠ 「赤」という文字列
```

である。

「赤」という単語は、人間が概念を表現するために使用している記号の一つにすぎない。

本研究が最終的に形成したいのは単語そのものではなく、その背後に存在し得る共通構造である。

---

## 0.3 概念核は人間が直接定義しない

本研究では、

```text id="z5z0w5"
red = 特定のベクトル
circle = 特定のベクトル
```

のように、人間が概念核を直接定義する方法を採用しない。

人間が概念核を決定すると、その人間の分類方法、文化、言語、先入観などがモデル内部の概念体系へ直接持ち込まれる可能性があるためである。

そのため、

> **概念核は観測された経験の中に存在する共通性から、モデル自身によって形成されるべきである。**

という立場を本研究では採用する。

---

## 0.4 AIにも概念の形を事前指定しない

一方で、AIに完全に自由な概念体系を形成させる場合、人間の概念体系とは大きく異なる内部構造が形成される可能性がある。

これは将来的に人間とAIを接続する際の問題となり得る。

しかしExperiment 001では、この問題を解決しようとはしない。

まず、

> **観測経験だけから何らかの再利用可能な共通構造が形成されるのか**

を調べる。

そのため、概念核を、

- 一点
- ベクトル
- クラスタ
- 領域
- 方向
- 部分空間
- 曲面
- 状態変化
- 変換
- その他の構造

のいずれかであると事前には決定しない。

例えば、

```text id="umhhdp"
丸 ─────────→ 赤い丸
四角 ───────→ 赤い四角
三角 ───────→ 赤い三角
        ↑
    共通する変化 R
```

という構造が形成された場合、「赤」に対応する概念核候補は一点ではなく、潜在表現を変化させる共通の**方向または変換**として存在している可能性もある。

---

## 0.5 概念核は固定された真理とは限らない

本研究では、形成された概念核が常に正しいとは仮定しない。

限られた経験しか存在しない場合、AIが異なる概念を同一のものとして扱う可能性がある。

例えば、

```text id="v8r6di"
経験：

赤い丸
赤い丸
赤い丸

        ↓

誤った内部構造：

「赤」 ≒ 「丸」
```

となる可能性がある。

その後、

```text id="ic5smh"
赤い四角
青い丸
赤い三角
```

などの反例を経験することで、

```text id="07evrx"
「赤」
  と
「丸」

は独立した構造なのではないか
```

と内部表現が再構成されることが望ましい。

したがって長期的には概念核を、

> **新しい経験によって生成・修正・分割・統合され得る内部仮説**

として扱う。

ただし、この修正能力そのものはExperiment 001の対象外とする。

---

## 0.6 本研究における暫定的な定義

以上を踏まえ、本研究では「概念核」を暫定的に以下のように定義する。

> **概念核とは、多数の異なる経験に共通して現れ、それらを説明・区別・再構成し、未知の状況においても再利用できる可能性を持つ内部構造である。**

この定義は概念核の**機能**を定義するものであり、その物理的・数学的な形を定義するものではない。

概念核が実際に、

```text id="bl1zm4"
点なのか
方向なのか
領域なのか
変換なのか
複数構造の組み合わせなのか
```

については実験結果から検討する。

---

## 0.7 「概念核候補」と「概念核」を区別する

本研究では、ニューラルネットワーク内部に何らかの規則的構造を発見しただけでは、それを概念核とは呼ばない。

例えば、

- PCA上でクラスタが形成された
- 同色画像の潜在距離が近かった
- 線形分類器が色を高精度で読み出せた

という結果だけでは、

> 「赤という概念核が形成された」

とは結論しない。

この段階では**「概念核候補」**と呼ぶ。

概念核と呼ぶためには、後続実験において少なくとも、

```text id="8ekzlx"
異なる状況で共通して利用できる

        ＋

未知の状況でも再利用できる

        ＋

単なる入力統計では説明しにくい
```

といった性質を検証する必要がある。

Experiment 001では、その前段階として**概念核候補となり得る内部構造が自然に形成されるか**を調べる。

---

# 1. 目的

本研究の長期的な目的は、人間が概念を直接定義することなく、観測された経験から再利用可能な共通構造を自律的に形成するAIを構築することである。

Experiment 001では、その最も基本的な問いを検証する。

**研究質問：**

> 単純な視覚経験のみから学習したニューラルネットワーク内部に、人間が「色」「形」と呼ぶ性質に対応する共通構造が自発的に形成されるか？

本実験では「概念核」の存在を証明することを目的としない。

まず、概念核候補となり得る潜在構造が形成されるかを観測する。

---

## 2. 仮説

単純図形を多数観測し、それらを圧縮・再構成するよう学習したニューラルネットワークでは、学習時に色・形ラベルを与えなくても、潜在空間に色や形に対応する何らかの規則的構造が形成される可能性がある。

ただし、その構造が以下のどれになるかは事前に決めない。

- 点
- クラスタ
- 方向
- 領域
- 部分空間
- 曲面
- 変換
- その他の構造

「概念核はこの形である」という前提をモデルに与えない。

---

# 3. 人工世界

## 3.1 画像

画像サイズ：

`64 × 64 px`

背景：

黒

画像中央付近に単一の図形を配置する。

---

## 3.2 色

3種類を使用する。

- 赤
- 緑
- 青

初期実装では明確に区別できるRGB値を使用する。

---

## 3.3 形

3種類を使用する。

- 円
- 三角形
- 四角形

---

## 3.4 組み合わせ

すべての組み合わせを生成する。

|     | 赤 | 緑 | 青 |
| --- | - | - | - |
| 円   | ○ | ○ | ○ |
| 三角形 | △ | △ | △ |
| 四角形 | □ | □ | □ |

合計9種類の基本組み合わせとなる。

---

# 4. 変動要素

モデルが画像そのものを丸暗記することを防ぐため、同じ「赤い円」でも画像ごとに若干異なる状態を生成する。

変更可能な要素：

- X座標
- Y座標
- 大きさ
- 三角形・四角形の回転角
- 微小なRGB変動

ただしExperiment 001では世界を複雑にしすぎない。

変動幅は設定ファイルから変更可能にする。

---

# 5. データセット

初期値として、

`9組み合わせ × 1,000画像 = 9,000画像`

を生成する。

データを以下に分割する。

- Train: 80%
- Validation: 10%
- Test: 10%

乱数seedを指定可能にする。

同一seedから同一データセットを再生成できること。

---

# 6. ラベルの扱い

生成プログラムは評価のために以下のメタデータを保持する。

例：

```text id="8ij8uo"
image_00001
color = red
shape = circle
x = ...
y = ...
size = ...
rotation = ...
```

ただし、

**colorおよびshapeをモデルの学習入力・教師信号として使用してはならない。**

これらは実験終了後の解析専用情報とする。

モデルから見えるのは基本的に画像のみである。

---

# 7. モデル

Experiment 001では小規模なAutoencoderを使用する。

構造：

```text id="t1b4mf"
64×64 RGB image
        ↓
     Encoder
        ↓
   Latent Space
        ↓
     Decoder
        ↓
Reconstructed Image
```

モデルの目的は入力画像を再構成することである。

---

# 8. 潜在空間

初期設定：

`latent_dim = 32`

ただし固定値とはせず設定可能にする。

後続実験で、

- 8
- 16
- 32
- 64
- 128

などを比較できる設計とする。

---

# 9. 損失関数

Experiment 001では画像再構成損失のみを使用する。

重要：

**「同じ色を近付ける」「同じ形を近付ける」といった損失を追加してはならない。**

それを行うと、人間が概念構造をモデルへ直接教えることになるためである。

---

# 10. 学習中に記録する情報

最低限、以下を記録する。

- epoch
- training loss
- validation loss
- random seed
- model configuration
- dataset configuration
- latent dimension
- optimizer configuration
- 学習済みモデル

実験を可能な限り再現できる状態にする。

---

# 11. 潜在表現の保存

TestデータについてEncoderの出力を保存する。

例：

```text id="j0a00y"
sample_id
latent_0
latent_1
...
latent_31

evaluation_metadata:
color
shape
position
size
rotation
```

色・形メタデータは、この段階になって初めて解析に使用してよい。

---

# 12. 解析

潜在表現について最低限以下を実施する。

### PCA

32次元の潜在表現を2次元・3次元へ射影する。

評価用メタデータを使い、

- 色別
- 形別

に可視化する。

### 距離解析

以下を比較する。

```text id="s9vteh"
同じ色・違う形
違う色・同じ形
同じ色・同じ形
違う色・違う形
```

潜在空間上の距離分布を調べる。

### 線形プローブ

潜在表現だけを入力として、単純な線形分類器に

- 色
- 形

を予測させる。

これはAutoencoder本体の学習後に行う。

線形プローブの結果をAutoencoderへ逆伝播してはならない。

目的は、

> 「潜在空間に色・形の情報がどの程度読み出し可能な形で存在するか」

を測定することである。

---

# 13. コントロール

結果を過大解釈しないため、少なくとも以下と比較する。

### 未学習モデル

ランダム初期化されたEncoderについて同じ解析を行う。

これによって、学習後に観測された構造がネットワーク初期化だけでも発生するものではないか確認する。

### Pixel baseline

可能であれば、生の画像データに対して簡単な基準解析を行う。

これにより、

> ニューラルネットワークが概念らしいものを形成した

のか、

> RGBピクセルの単純な統計だけで分類できただけ

なのかを区別する材料とする。

---

# 14. 成功条件

Experiment 001では、以下だけで「概念核を発見した」とは判定しない。

- PCAで綺麗に分かれた
- 色分類精度が高かった
- それらしいクラスタが存在した

これらは潜在構造の存在を示す証拠にはなり得るが、概念核そのものの証明とはしない。

Experiment 001の成功条件は、

> 学習前と比較して、学習後の潜在空間に色または形に対応する再現可能な規則的構造が形成されていること。

とする。

複数seedで同様の傾向が観測されることが望ましい。

---

# 15. 失敗も結果として扱う

以下の結果も正式な実験結果として扱う。

- 色だけ分離された
- 形だけ分離された
- 色と形が混ざった
- 潜在構造が見つからなかった
- seedによって構造が大きく変わった
- 再構成は成功したが意味のある構造が形成されなかった

仮説に合わない結果を除外してはならない。

---

# 16. Experiment 001では行わないこと

以下は後続実験に回す。

- 言語
- Transformer / LLM
- 実写画像
- 概念核の明示的生成
- 概念核の統合
- 概念核の分割
- 誤認からの修正
- 継続学習
- 未知概念の発見
- 人間との会話
- 現実課題の解決

Experiment 001では、

**「共通構造が自然に形成されるか」**

だけを見る。

---

# 17. 再現性

すべての実験について、

```text id="tr2fvs"
experiment_id
git_commit
seed
dataset_config
model_config
training_config
results
```

を記録する。

実験結果だけでなく、失敗した実験も保存する。

---

# 18. Experiment 001の最終成果物

Experiment 001の成果物は、原則として1回の実験実行ごとに独立して保存する。

例：

```text id="9i8wop"
exp001/
└── runs/
    └── run_001/
        ├── config.yaml
        ├── dataset_metadata.csv
        ├── checkpoints/
        │   ├── initial.pt
        │   └── final.pt
        ├── training_log.csv
        ├── latent_vectors.csv
        ├── pca_color.png
        ├── pca_shape.png
        ├── distance_analysis.csv
        ├── linear_probe_results.json
        └── report.md
```

複数のseedや設定を試す場合は、`run_002`、`run_003` のように別ディレクトリへ保存し、既存の実験結果を上書きしない。

## 18.1 `config.yaml`

その実験を再現するために必要な設定値を保存する。

最低限、以下を含める。

```text id="9txxt9"
experiment_id
run_id
git_commit
seed

dataset:
  image_size
  samples_per_combination
  train_ratio
  validation_ratio
  test_ratio
  colors
  shapes
  position_range
  size_range
  rotation_range
  color_variation

model:
  architecture
  latent_dim

training:
  epochs
  batch_size
  optimizer
  learning_rate
  loss_function
```

実験開始後に設定値が変更された場合、最終的に**実際に使用された値**を保存する。

---

## 18.2 `dataset_metadata.csv`

生成された全サンプルについて、評価および再現に必要なメタデータを保存する。

1行を1画像とする。

最低限、以下の列を持つ。

```text id="x9jx89"
sample_id
split
color
shape
x
y
size
rotation
r
g
b
generation_seed
```

`split` は、

```text id="mujekb"
train
validation
test
```

のいずれかとする。

`color` と `shape` は評価専用情報であり、Autoencoderの学習入力または教師信号として使用してはならない。

---

## 18.3 `checkpoints/`

モデルの状態を保存する。

最低限、

```text id="zxfq05"
initial.pt
final.pt
```

を保存する。

### `initial.pt`

学習開始前のランダム初期化されたモデルを保存する。

未学習モデルとの比較解析に使用する。

### `final.pt`

学習終了時点のモデルを保存する。

可能であれば、モデルパラメータだけでなく、

```text id="t7q5ra"
model_state_dict
optimizer_state_dict
epoch
training_loss
validation_loss
```

など、学習状態を再現するために必要な情報も保存する。

後続実験で必要になった場合は、中間epochのcheckpoint保存を追加してよい。

---

## 18.4 `training_log.csv`

学習過程を1 epochにつき1行で保存する。

最低限、

```text id="khkg3c"
epoch
training_loss
validation_loss
```

を含める。

必要に応じて、

```text id="i2e1no"
learning_rate
epoch_duration
```

などを追加してよい。

このファイルから学習曲線を再構成できること。

---

## 18.5 `latent_vectors.csv`

TestデータをEncoderへ入力した際の潜在表現を保存する。

1行を1サンプルとする。

`latent_dim = 32` の場合、

```text id="z9e0js"
sample_id
latent_0
latent_1
...
latent_31
color
shape
x
y
size
rotation
```

を保存する。

`color`、`shape` 等は解析用メタデータとして付加する。

潜在ベクトルそのものと評価用情報を同じ行から対応付けられる状態にする。

---

## 18.6 `pca_color.png`

Testデータの潜在表現をPCAで低次元へ射影した結果を保存する。

各サンプルを**色の評価ラベル**によって区別して表示する。

この図は、

> 色に対応する構造が潜在空間に存在する可能性があるか

を視覚的に確認する補助資料とする。

PCA図だけを根拠に概念形成を断定してはならない。

---

## 18.7 `pca_shape.png`

`pca_color.png` と同一のPCA結果を使用し、各サンプルを**形の評価ラベル**によって区別して表示する。

PCA自体を別々に計算して比較不能な図を作るのではなく、原則として同じ射影座標を使用する。

これによって、

```text id="2rc7cm"
色による構造
        と
形による構造
```

を同じ潜在空間上で比較できるようにする。

---

## 18.8 `distance_analysis.csv`

潜在空間におけるサンプル間距離の解析結果を保存する。

最低限、

```text id="n6yrnl"
same_color_same_shape
same_color_different_shape
different_color_same_shape
different_color_different_shape
```

の4条件について、

```text id="nx35xx"
count
mean_distance
median_distance
standard_deviation
```

を記録する。

必要に応じて追加統計量や個々の距離データを別ファイルとして保存してよい。

距離尺度を変更した場合は、その種類を記録する。

---

## 18.9 `linear_probe_results.json`

学習済みEncoderの潜在表現から、

- color
- shape

をそれぞれ予測した線形プローブの結果を保存する。

最低限、

```text id="mv6vzu"
color_accuracy
shape_accuracy
```

を保存する。

可能であれば、

```text id="zuzfby"
chance_level
train_accuracy
test_accuracy
confusion_matrix
probe_configuration
```

も保存する。

さらに、未学習Encoderに対して同じ線形プローブを実行した結果も記録し、

```text id="laj17p"
untrained_encoder
trained_encoder
```

を直接比較できる形式とする。

線形プローブの学習結果をAutoencoderへ逆伝播してはならない。

---

## 18.10 `report.md`

Experiment 001の人間向け実験報告書を保存する。

最低限、以下の構成とする。

### 1. 実験条件

使用したデータセット、モデル、seed、主要ハイパーパラメータを記録する。

### 2. 学習結果

training loss、validation loss、再構成結果について記録する。

### 3. 潜在空間解析

PCA、距離解析、線形プローブの結果をまとめる。

### 4. 観測された構造

潜在空間にどのような規則性が観測されたかを記述する。

### 5. 仮説と一致した点

事前仮説と整合した結果を記録する。

### 6. 仮説と一致しなかった点

予想外の結果や仮説に反する結果を記録する。

### 7. 解釈上の注意

結果から断定できないこと、交絡要因、実験上の限界を記録する。

### 8. 次の実験への示唆

Experiment 002以降で検証すべき事項を記録する。

`report.md`では、結果と解釈を可能な限り区別する。

例えば、

```text id="h0pp9e"
観測：
同色・異形状の平均距離が異色・同形状より小さかった。

解釈：
潜在表現が形より色を強く保持している可能性がある。
```

のように、「測定された事実」と「そこから考えられる説明」を混同しない。

---

# 19. 研究上の原則

本実験では以下を守る。

**・概念をモデルに直接教えない。**

**・概念核の形を事前に決めない。**

**・可視化だけを根拠に概念の存在を断定しない。**

**・評価用ラベルと学習用情報を明確に分離する。**

**・仮説に反する結果も保存する。**

**・再現可能性を重視する。**

---

## Experiment 001の問い

最終的に、この実験が答えるべき問いは以下の一つである。

> **人間が「赤」「青」「円」「四角」といった概念を一切教師として与えなくても、単純な視覚経験を再構成する過程で、それらに対応する共通構造はAI内部に自発的に形成されるのか？**
