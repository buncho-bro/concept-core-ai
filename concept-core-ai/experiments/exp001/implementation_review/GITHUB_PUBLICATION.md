# GitHub publication copy

Implementation and verification evidence are published under the existing `concept-core-ai/` directory. Existing research specifications are unchanged.

Personal absolute paths, hostnames and OS details have been removed from published verification logs/receipts. Earlier Windows-encoded logs are converted to UTF-8 for publication. Original local evidence is retained.

Test outcomes, test counts, library versions and implementation fingerprints are preserved. The prior verification report describes the local state at verification time; its statement that no push had occurred is historical.

Published receipts contain redacted environment fields. Run `python -m exp001 verify --output .verify-tmp/check-001` after checkout to obtain a local receipt before a later authorized MODE: RUN. No formal baseline was executed for this publication.
